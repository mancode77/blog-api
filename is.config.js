// Sync object
/** @type {import('@jest/types').Config.InitialOptions} */
export const config = {
    verbose: true,
};

// Or async function
export default async () => {
    return {
        verbose: true,
    };
};